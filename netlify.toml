[build]
  base = "frontend"
  publish = "build"
  command = "CI=false npm run build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Cloudflare Pages usa este mesmo arquivo.
# O Worker (backend) fica em /worker e é deployado separadamente via wrangler.
